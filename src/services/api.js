/*
import axios from "axios";

// Функция для получения данных
export const fetchData = () => {
    return axios.get('http://localhost:4000/api/data');
};

// Другие функции для выполнения других запросов, если необходимо

*/
